1️⃣ What is the difference between var, let, and const?

Ans:1.var can redeclare & reassign
2.let can reassign but cannot redeclare in same scope
3.const cannot reassign or redeclare

2️⃣ What is the spread operator (...)?

Ans:The spread operator is used to expand the elements of an array, string, or object into individual elements

3️⃣ What is the difference between map(), filter(), and forEach()?

Ans:1.map()	transforms every item
2.filter() selects specific items
3.forEach()	Acts on every item

4️⃣ What is an arrow function?

Ans:An arrow function is a shorter way to write functions

5️⃣ What are template literals?

Ans:Template literals are a modern way to create strings